apiVersion: v1
kind: Service
metadata:
  name: kube-calc-service
  labels:
    app: kube-calc
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: kube-calc
